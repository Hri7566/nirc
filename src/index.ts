import { Client, ClientEvents, ChannelName, IRCConfig, MessageTypes } from './Client'

export default Client

export {
    Client,
    ClientEvents,
    ChannelName,
    IRCConfig,
    MessageTypes
}
